<div id="clearance" class="sign-up">
  <h2><%= t(".title") %></h2>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>

    <div class="submit-field">
      <%= form.submit %>
    </div>

    <div class="other-links">
     <p>Have an account already?<%= link_to t(".sign_in"), sign_in_path %></p>
    </div>
  <% end %>
</div>
